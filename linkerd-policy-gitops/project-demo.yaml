projects:
  demo:
    grants:
      rabbitmq-amqp:
        serviceAccounts:
          - namespace: rabbitmq
            name: rabbitmq-spa-demo
      rabbitmq-management:
        serviceAccounts:
          - namespace: rabbitmq
            name: rabbitmq-spa-demo
